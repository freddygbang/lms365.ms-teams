export interface Course {
    description: string;
    id: string;
    imageUrl: string;
    longDescription: string;
    title: string;
    url: string;
}

export interface CourseCatalog {
    id: string;
    title: string;
    url: string;
}